#!/bin/sh

PROJECT_DIR=$(cd `dirname $0` && pwd)/..

cd ${PROJECT_DIR}

rm -f ${PROJECT_DIR}/tmp/prove.log

prove -r --ext=.phpt --exec 'php -n -c ./etc/phpt.ini -d include_path=./lib -f' t/

cd - > /dev/null
