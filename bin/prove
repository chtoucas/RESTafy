#!/bin/sh

PROJECT_DIR=$(cd `dirname $0` && pwd)/..

cd ${PROJECT_DIR}

rm -f ${PROJECT_DIR}/test.log

if [ "$1" = "" ]; then
    prove -r --ext=.phpt --exec 'php -n -c ./etc/phpt.ini -f' t/
else
    php -n -c ./etc/phpt.ini -f "$1"
fi

cd - > /dev/null
