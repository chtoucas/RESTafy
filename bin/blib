#!/bin/sh
# TODO:
# - clean

PROJECT_DIR=$(cd `dirname $0` && pwd)/..
BLIB_DIR="${PROJECT_DIR}/blib"

[ -d ${BLIB_DIR} ] || mkdir ${BLIB_DIR}

rm -rf ${BLIB_DIR}/*

php -w lib/RESTafy.php > ${BLIB_DIR}/RESTafy.php
php -w lib/Test.php > ${BLIB_DIR}/Test.php
